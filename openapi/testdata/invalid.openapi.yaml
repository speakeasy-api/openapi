openapi: 3.1.0
info:
  title: Invalid API
  # Missing required version field
paths:
  /users/{id}:
    get:
      summary: Get user by ID
      parameters:
        - name: id
          in: path
          required: true
          # Missing schema definition
        - name: invalidSchema
          in: query
          schema:
            $ref: "#/components/schemas/InvalidSchema"
        - name: invalidNullSchema
          in: query
          schema:
            type:
              - null # Invalid null value instead of "null" string
              - string
      responses:
        "200":
          description: User found
          content:
            application/json:
              # Missing schema definition
    post:
      responses: {} # Missing default response
  /invalid:
    post:
      parameters:
        - name: invalidSchema
          in: query
          schema:
            type: object
            properties:
              type:
                type: string
              required: # at wrong level
                - name
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: invalid_type # Invalid schema type
      # Missing required responses field
components:
  schemas:
    InvalidSchema:
      type: object
      properties:
        # Incorrectly defined properties
        name: string
        example: "John Doe"
      examples:
        # non-sequence based examples
        Example 1:
          syncId: 1ad0695c-4566-4715-918c-adbb03eac81e
