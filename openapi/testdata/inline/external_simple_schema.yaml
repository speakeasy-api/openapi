# Simple external schema without circular references
type: object
required:
  - id
  - name
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
    minLength: 1
    maxLength: 100
  description:
    type: string
    maxLength: 500
  created_at:
    type: string
    format: date-time
