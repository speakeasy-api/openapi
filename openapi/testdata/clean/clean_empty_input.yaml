openapi: 3.1.0
info:
  title: Empty API
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        "200":
          description: Success
components:
  schemas:
    UnusedSchema:
      type: string
  parameters:
    UnusedParam:
      name: unused
      in: query
      schema:
        type: string
